<%# JL - 2013.08.15 - form_for.error_messages was deprecated in Rails 3.  Replaced with this "partial" as per Ruby on Rails Tutorial P309%>
<% if @application.errors.any? %>
  <div id="errorExplanation">
    <div class="errorExplanation">
		<p><%= pluralize(@application.errors.count, "error") %> prohibited this application from being saved.
		<br/>There were problems with the following fields:</p>
    <ul>
    <% @application.errors.full_messages.each do |msg| %>
      <li>* <%= msg %></li>
    <% end %>
    </ul>
    </div>
  </div>
<% end %>